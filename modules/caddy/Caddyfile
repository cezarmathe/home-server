(defaults_public) {
    encode zstd gzip
    tls ${cf_email} {
        dns cloudflare ${cf_api_token}
    }
}

%{ for service in public_services ~}
${service.hostname} {
    import defaults_public
    reverse_proxy ${service.address}
}
%{ endfor ~}
